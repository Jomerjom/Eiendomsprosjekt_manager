import { createPropertyCard } from "./JS/createPropertyCard.js";
import { createProjectList } from "./JS/createProjectList.js";
import { timestampConverter } from "./JS/timestampConverter.js";
// Build the website by running the createProjectList and createPropertyCard functions
createProjectList();
createPropertyCard(0);